query findUser($username: String!) {
    findUser(username: $username) {
        username
        firstName
        lastName
        email
        id
        birthDate
        gender
        title
    }
}